[package]
name = "bash-pinyin-completion-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
ib-matcher = { version = "0.4.0", features = ["pinyin"] }
regex = "1.11.1"

[dev-dependencies]
assert_cmd = "2.0.17"
predicates = "3.1.3"
